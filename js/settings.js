(() => {
  function init(){const page=document.getElementById('settings');if(page)page.dataset.module='settings';}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})();
