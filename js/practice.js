(() => {
  function init(){const page=document.getElementById('practice');if(page)page.dataset.module='practice';}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})();
